{
    "name": "Windows Cross-Compile Environment",
    "image": "mcr.microsoft.com/devcontainers/base:bullseye",
    
    "runArgs": [
        "--platform", "linux/amd64"
    ],

    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y nasm mingw-w64"
}